Review this conversation and save anything worth keeping to the project memory for the current project. The memory path follows the pattern `/home/john/.claude/projects/<slug>/memory/` where `<slug>` is the current project's directory path with slashes replaced by hyphens (e.g. working in `~/repos/vivify-inferences` → slug is `-home-john-repos-vivify-inferences`). Update existing memory files if relevant, create new ones for new topics, and keep MEMORY.md index in sync. Focus on: design decisions, new scripts or tools added, philosophical framing that evolved, setup state changes, and any feedback on how to collaborate. Skip anything already captured or that won't matter next session.
